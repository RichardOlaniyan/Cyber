# **Day 2: File Hasher Tool**
- **Objective**: Build a command-line tool that calculates and compares hashes (e.g., MD5, SHA-256) of files to ensure integrity.
- **Concepts**: `hashlib module`, `file I/O`, `command-line processing`.
